mixin realtorsList
   .realtors__list 
      each realtor, i in realtors
         img.realtors__photo(src=`assets/img/realtor-${i + 1}.jpeg`, alt=`Realtor ${i + 1}`)
         .realtors__details
            h4.title-4.title-4--light.realtors__title #{realtor.name}
            p.realtors__sold #{realtor.sold} Houses sold